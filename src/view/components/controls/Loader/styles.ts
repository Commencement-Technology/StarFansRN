import { StyleSheet } from "react-native";

export default StyleSheet.create({
  loader: {
    height: 34,
    width: 34
  },
  wrapper: {
    alignItems: "center",
    justifyContent: "center"
  }
});
