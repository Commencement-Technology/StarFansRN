import { StyleSheet } from "react-native";

export default StyleSheet.create({
  background: {
    bottom: 0,
    left: 0,
    position: "absolute",
    right: 0,
    top: 0,
    zIndex: -1
  },
  wrapper: {
    borderColor: "#788596",
    borderRadius: 16,
    borderWidth: 1.5,
    overflow: "hidden",
    padding: 16,
    position: "relative"
  }
});
